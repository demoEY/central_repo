name: Invoke Parameterized Hello World

on:
  push:
    branches:
      - main

jobs:
  invoke_hello_world:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Hello World workflow
        uses: demoEY/central_repo/.github/workflows/hello_world_workflow.yml@main
        with:
          message: "Hello from invoking workflow!"
          recipient: "Anshul"
